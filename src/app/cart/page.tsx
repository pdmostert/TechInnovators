export default function CartPage() {
  return (
    <main style={{ maxWidth: 720, margin: "3rem auto", padding: "0 1rem" }}>
      <h1 style={{ marginBottom: "0.75rem" }}>Shopping Cart</h1>
      <p>
        Cart flow is scheduled for later phases. This route is in place so the
        primary navigation is stable during Week 3.
      </p>
    </main>
  );
}
